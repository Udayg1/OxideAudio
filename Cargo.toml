[profile.release]
opt-level = 3
codegen-units=256
lto = "thin"
panic = "abort"
strip = "symbols"

[profile.dev]
incremental = true
codegen-units=256


[workspace]
resolver = "3"
members = [
    "app",
    "macros",
]
